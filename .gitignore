# ========== 1. 忽略 UE 自动生成的冗余目录 ==========
Binaries/          # 编译生成的二进制可执行文件
Intermediate/      # 编译中间临时文件
DerivedDataCache/  # 引擎衍生的缓存数据
Saved/             # 运行时自动生成的配置/日志等

# # ========== 2. 忽略 Content 下的资源文件（模型、贴图、蓝图逻辑等） ==========
# # 规则：Content 目录及其所有子目录下的 uasset/uexp/ubulk/uptnl 文件都忽略
# Content/**/*.{uasset,uexp,ubulk,uptnl}

# # ========== 3. 单独保留 umap 文件（不忽略） ==========
# # 规则：Content 目录及其所有子目录下的 umap 文件，取消上面的“忽略”规则
# !Content/**/*.umap

# ========== 4. （可选）忽略其他自定义非必要文件 ==========
# 例如项目配置、IDE 缓存等，根据实际需求补充
*.sln
*.vcxproj*
*.opendb
.idea/
.vscode/
*.uasset
/Saved
/Intermediate  

/Content
/Saved
/Saved
*.umap
*.umap
